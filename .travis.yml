language: java
jdk:
  - openjdk8

before_deploy:
  - export TRAVIS_TAG=$(git log --format=%h -1)
  - git tag $TRAVIS_TAG

deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file:
    - "main/target/robot-skeleton*.jar"
  skip_cleanup: true
  on:
    branch: master